When installing this app on iOS, do not use the app immediately after Xcode installed it and started it. Close the app fully, so that the Xcode debugger loses connection to it and restart it. If this is not done, Xcode lags the App to death.
